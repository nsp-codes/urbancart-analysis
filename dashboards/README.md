# Dashboard Folder

Use this folder to save your final dashboard file.

Recommended file names:

- `urbancart_dashboard.xlsx` for Excel
- `urbancart_dashboard.pbix` for Power BI
- `urbancart_dashboard.twbx` for Tableau

Use `data/processed/dashboard_ready.csv` as the dashboard input.

Keep it to one dashboard page only.
